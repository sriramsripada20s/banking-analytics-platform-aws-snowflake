# Airflow + Cosmos Orchestration

## Setup (after cloning)
1. `cd orchestration/airflow-cosmos`
2. `.\setup.ps1` (copies this repo's dbt models into place locally)
3. `astro dev start`

Requires: Docker Desktop running, Astro CLI installed.
